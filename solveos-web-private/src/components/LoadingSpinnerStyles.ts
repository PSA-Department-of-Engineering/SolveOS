import type { SxProps, Theme } from "@mui/material";

export const loadingSpinnerContainerStyles: SxProps<Theme> = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
};
